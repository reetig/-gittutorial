#include "common.h" // Include the common header

// Function to create and initialize a Point
Point createPoint(int x, int y)
{
    Point p;  // Declare a Point variable
    p.x = x;  // Set x coordinate
    p.y = y;  // Set y coordinate
    return p; // Return the initialized Point
}
